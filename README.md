# meta-quest-black-screen-fix
Meta Quest black-screen workaround for headsets where Guardian, tracking, audio, and casting still work. Uses ADB to bypass a broken color-calibration path—no root, firmware flashing, or data wipe.
